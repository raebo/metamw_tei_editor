interface DateDialogProps {
  onClose: () => void; // Function to close the dialog
}

const DateWhenAddDialog = (props: DateDialogProps) => {

  return (
    <>
      <h2>Datum</h2>
    </>
  )
}

export default DateWhenAddDialog;
